import Header from "./Header";

export const Default = {};

const headerConfig = {
  title: "Molecules/Header",
  component: Header,
  tags: ["autodocs"],
  excludeStories: /.*Data$/,
};

export default headerConfig;
